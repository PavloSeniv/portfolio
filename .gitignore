# dependencies
**/node_modules
**/.idea
**/gulp/config/ftp.js
# ------ Up Here ------

# OS
.DS_Store
**/.DS_Store

# Примітка: рядок `docs` прибрано — вся тека docs/ уже під версійним контролем
# (5944 файли), тож ігнор на неї не діяв і лише ховав нові файли від git status.

# --- CV: робочі документи лишаються ПОЗА git ---
# Репозиторій публічний, а ці три файли — не резюме, а кухня під нього:
# у playbook лежить таблиця git-ідентичностей із корпоративними поштами
# (зокрема чужою) і перелік приватних репозиторіїв роботодавця, у research —
# розбір тих самих приватних проєктів за кодом і git-історією.
# Самі файли живуть в Obsidian-сховищі Work/CV (воно приватне) — там вони
# закомічені повністю. Тут — лише те, що й так показують роботодавцям.
CV/cv-research.md
CV/cv-update-playbook.md
CV/TODO.md
